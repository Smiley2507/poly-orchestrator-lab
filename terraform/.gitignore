# Local Terraform state and working files - never commit these
# (.terraform.lock.hcl IS committed on purpose - it pins provider versions)
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log

# Real variable values (may contain the DB password) - only *.tfvars.example is committed
terraform.tfvars
*.auto.tfvars
